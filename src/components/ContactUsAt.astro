---
import { ButtonVariantProps } from './button-variants';
import Button from './Button.astro';

export interface Props {
  color: string;
  svg: string;
  title: string;
  description: string;
  link: string;
  buttonText: string;
}

const { color, svg, title, description, link, buttonText } = Astro.props;

// Map the color classes to button colors
function getButtonColor(color: string): ButtonVariantProps['color'] {
  if (color.includes('jpmc-blue')) return 'blue';
  if (color.includes('jpmc-red')) return 'red';
  if (color.includes('jpmc-gold')) return 'gold';
  if (color.includes('purple')) return 'blue'; // Instagram uses blue
  if (color.includes('black')) return 'gray';
  return 'blue'; // Fallback
}

const buttonColor = getButtonColor(color);
---

<div
  class="bg-white dark:bg-jpmc-gray-900 p-6 rounded-lg shadow-lg text-center hover:shadow-xl transition-shadow duration-300 w-full max-w-3xs"
>
  <div
    class={`w-16 h-16 ${color} rounded-full flex items-center justify-center mx-auto mb-4`}
  >
    <svg
      class="w-8 h-8 text-white"
      fill="currentColor"
      viewBox="0 0 24 24"
      set:html={svg}
    />
  </div>
  <h3 class="text-xl font-bold text-jpmc-dark dark:text-jpmc-gray-100 mb-2">{title}</h3>
  <p class="text-gray-600 dark:text-jpmc-gray-400 mb-4">{description}</p>
  <Button 
    href={link} 
    style="primary" 
    color={buttonColor} 
    size="md"
    target={link.startsWith('mailto:') ? undefined : "_blank"}
    rel={link.startsWith('mailto:') ? undefined : "noopener noreferrer"}
  >
    {buttonText} →
  </Button>
</div>
